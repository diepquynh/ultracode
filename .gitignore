# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Node dependency for the bundled ultracode_gate MCP server (mcp/gate-server.js) —
# installed locally for testing; the shipped plugin gets its own via package-lock.json
node_modules/

# Harness plugin distributions — generated from the neutral sources by install.sh
/dist/

# Local experiments — never ship generated per-repo output with the plugin
/sandbox/
/tmp/

# Orchestrator / init-kit working session scratch (per-run subdirs)
/.ultracode/session/
