{
  "name": "ultracode-mcp",
  "version": "1.1.0",
  "private": true,
  "description": "Bundled MCP server for the ultracode plugin: ultracode_gate/report/memory core tools plus the machine-level cross-harness hub (session registry, messaging, task distribution) served over stdio (hub-shim) and streamable HTTP (hub-server).",
  "license": "MIT",
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.0"
  }
}
