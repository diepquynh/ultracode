{
  "schema_version": 1,
  "kind": "command",
  "name": "hub-listen",
  "description": "Registers this session with the machine-level ultracode hub as a worker, then claims and runs the tasks orchestrator sessions on other harnesses publish. Invoke only when the user explicitly turns listening on in this session.",
  "prompt": "prompt.md",
  "config": {
    "argument_hint": "[optional: capabilities to claim, e.g. 'implementer review', or a display name]"
  }
}
