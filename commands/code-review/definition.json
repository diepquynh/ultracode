{
  "schema_version": 1,
  "kind": "command",
  "name": "code-review",
  "description": "Spawn ultracode:code-reviewer directly — review the working tree's changes against this repo's Review Rule Set and run the fix loop.",
  "prompt": "prompt.md",
  "config": {
    "argument_hint": "[implementation | test | full] [| scope: unstaged]"
  }
}
