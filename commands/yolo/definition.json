{
  "schema_version": 1,
  "kind": "command",
  "name": "yolo",
  "description": "Toggles YOLO mode for a primary session: standing permission to settle implementation-phase questions without asking, so an unattended run finishes the approved plan. Invoke when the user asks to turn YOLO on or off, or to check whether it is on.",
  "prompt": "prompt.md",
  "config": {
    "argument_hint": "on | off | status [optional note, e.g. 'finish D2 overnight']"
  }
}
